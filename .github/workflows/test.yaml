name: Test

on:
  push:
    branches-ignore:
      - "main"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v4

      - name: Build docker image 👷⚙️
        run: make build

      - name: Run unit tests 👨‍🔬🧪
        run: make test-unit
