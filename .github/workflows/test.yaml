name: Test

on:
  push:
    branches-ignore:
      - "main"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v4

      - name: Install dependencies ⚙️
        uses: actions/setup-python@v5
        with:
            python-version: '3.12.4'

      - name: Install dependencies ⚙️
        run: pip install pipenv

      - name: Run tests 🧪
        run: pytest -svv --cov=src/ tests
